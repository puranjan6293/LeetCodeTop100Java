package TOP_QUESTIONS.Tree;

public class kth_smallest_element_in_BST {
    /*
    int count = 0;
    int ans = 0;
    public int kthSmallest(TreeNode root, int k) {
        //Approach: Using dfs inorder traversal
        inorder(root, k);
        return ans;
    }
    public void inorder(TreeNode root, int k){
        //base case 
        if(root==null) return;

        //left subtree
        inorder(root.left, k);

        //if condition satisfy
        if(++count==k){
            //assign the root.val to ans
            this.ans = root.val;
        }

        //right subtree
        inorder(root.right, k);
    }
     */
}
