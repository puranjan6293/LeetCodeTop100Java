package TOP_QUESTIONS.Recursion;

public class Remove_Nodes_from_Linkedlist {
    /*
    public ListNode removeNodes(ListNode head) {
        //Approach, Using Recurrsion, T.C: O(n)

        //edge case
        if(head.next==null) return head;

        //traverse
        head.next = removeNodes(head.next);
        //condition
        if(head.next.val>head.val) return head.next;

        //return head
        return head;
    }
     */
}
